[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "synthetic-gov-data-kit"
version = "0.1.0"
description = "Generate structured synthetic US government benefits data for LLM reasoning evaluation"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.10"
authors = [
    { name = "CivBench Contributors" }
]
keywords = ["synthetic-data", "government", "benefits", "llm", "evaluation", "snap", "wic", "medicaid"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "Intended Audience :: Developers",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]

dependencies = [
    "pydantic>=2.0",
    "pyyaml>=6.0",
    "httpx>=0.27",
    "rich>=13.0",
    "jinja2>=3.1",
    "faker>=24.0",
    "python-dateutil>=2.9",
    "typing-extensions>=4.9",
    "typer>=0.12",
]

[project.optional-dependencies]
hf = [
    "datasets>=2.19",
    "huggingface-hub>=0.23",
]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "pytest-asyncio>=0.23",
    "ruff>=0.4",
    "mypy>=1.10",
    "types-PyYAML>=6.0",
    "pre-commit>=3.7",
    "ipykernel>=6.29",
    "jupyter>=1.0",
    "respx>=0.21",
]
all = [
    "synthetic-gov-data-kit[hf,dev]",
]

[project.scripts]
govsynth = "govsynth.cli.main:app"

[project.urls]
Homepage = "https://github.com/ctrimm/synthetic-gov-data-kit"
Documentation = "https://github.com/ctrimm/synthetic-gov-data-kit/tree/master/docs"
Repository = "https://github.com/ctrimm/synthetic-gov-data-kit"
Issues = "https://github.com/ctrimm/synthetic-gov-data-kit/issues"

[tool.hatch.build.targets.wheel]
packages = ["govsynth"]

[tool.ruff]
line-length = 100
target-version = "py310"
src = ["govsynth", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP", "ANN", "B", "SIM"]
# ANN101/ANN102 are deprecated; ANN401 (disallow `Any`) is intentionally not enforced —
# `Any` is used deliberately at dynamic boundaries (pipeline wiring, formatters, JSON I/O).
ignore = ["ANN101", "ANN102", "ANN401"]

[tool.ruff.lint.per-file-ignores]
# Tests, notebooks, and dev scripts are not shipped. Style rules (annotations, line
# length, import sorting, illustrative idioms) add noise there; real errors (F8xx
# undefined/unused names) are still enforced.
"tests/**" = ["ANN", "E501"]
"notebooks/**" = ["ANN", "E501", "E401", "E402", "F401", "F811", "I", "B"]
"scripts/**" = ["ANN", "E501"]

[tool.mypy]
python_version = "3.10"
strict = true
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = "--cov=govsynth --cov-report=term-missing"

[tool.coverage.run]
source = ["govsynth"]
omit = ["tests/*"]
